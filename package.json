{
  "name": "ecommerce-react-express",
  "version": "1.0.0",
  "description": "A fullstack e-commerce platform built with React + Express, featuring a backend API and a frontend client",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "install-all": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client"
  },
  "keywords": [],
  "author": "Ashrafin Hasib",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
